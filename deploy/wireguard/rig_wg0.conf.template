# SAIREN Fleet Rig — WireGuard Configuration Template
# Copy to /etc/wireguard/wg0.conf and fill in keys
#
# Generate keys:
#   wg genkey | tee rig_private.key | wg pubkey > rig_public.key
#
# Start: wg-quick up wg0
# Enable: systemctl enable wg-quick@wg0

[Interface]
Address = 10.0.1.X/32
PrivateKey = <RIG_PRIVATE_KEY>

[Peer]
PublicKey = <HUB_PUBLIC_KEY>
Endpoint = <HUB_PUBLIC_IP>:51820
AllowedIPs = 10.0.0.1/32
PersistentKeepalive = 25
